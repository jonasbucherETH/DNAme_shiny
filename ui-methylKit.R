tabItem(
  tabName = "tab-methylKit",
  fluidRow( ### NOTE: 1 row has width = 12
    column(
      width = 2,
      box(
      ) # close box
    ) # close column
  ) # close fluidRow
) # close tabItem

